# ==============================================================================
#  Customer Churn Intelligence Platform — Python Dependencies
# ==============================================================================
#
#  Install with:
#      pip install -r requirements.txt
#
#  Recommended Python version: 3.9 – 3.12
# ==============================================================================

# ── Core Data & ML ─────────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# ── Excel Report Generation ────────────────────────────────────────────────────
xlsxwriter>=3.1.0          # writes .xlsx with charts, formats, merged cells
openpyxl>=3.1.0            # reads .xlsx / .xlsm input files
xlrd>=2.0.1                # reads legacy .xls files
# NOTE: Limited .xls support in newer versions

# ── Optional — ODS support (uncomment if needed) ───────────────────────────────
# odfpy>=1.4.1              # reads .ods spreadsheets (OpenDocument)
# Uncomment if using .ods files

# ── GUI (bundled with Python — no install needed, listed for clarity) ──────────
# tkinter                   # part of the Python standard library

# ── Packaging / Distribution (only needed when building an .exe) ───────────────
# pyinstaller>=6.0.0        # pip install pyinstaller  →  pyinstaller churn_analytics_gui.py --onefile --windowed
