# ==============================================================================
#  Customer Churn Intelligence Platform — Python Dependencies
#  Author   : Abhishek
#  Version  : v3.0 (Final)
# ==============================================================================

# ── Core Data Processing ───────────────────────────────────────────────────────
pandas>=1.3.0
numpy>=1.21.0

# ── Machine Learning ───────────────────────────────────────────────────────────
scikit-learn>=0.24.0

# ── Excel Report Generation ────────────────────────────────────────────────────
xlsxwriter>=3.0.0        # Writes the formatted .xlsx output report

# ── Excel / Spreadsheet Reading ────────────────────────────────────────────────
openpyxl>=3.0.0          # Reads .xlsx, .xlsm, .xlsb files
xlrd>=2.0.0              # Reads legacy .xls files
odfpy>=1.4.0             # Reads .ods (OpenDocument Spreadsheet) files

# ── GUI (Tkinter is built into Python's standard library) ──────────────────────
# tkinter — no pip install needed; ships with Python 3.x on all platforms.
# If missing on Linux, install via system package manager:
#   Ubuntu/Debian : sudo apt-get install python3-tk
#   Fedora/RHEL   : sudo dnf install python3-tkinter

# ── Notes ──────────────────────────────────────────────────────────────────────
# Python 3.8 or higher is required.
# Install all dependencies with:
#   pip install -r requirements.txt
